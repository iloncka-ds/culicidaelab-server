# Docker environment configuration for backend
# This file contains default environment variables for Docker deployment

# Application settings
CULICIDAELAB_DATABASE_PATH="/app/backend/data/.lancedb"
CULICIDAELAB_SAVE_PREDICTED_IMAGES="false"
CULICIDAELAB_BACKEND_CORS_ORIGINS="http://localhost:8765,http://127.0.0.1:8765,http://frontend:8765"

# Environment
ENVIRONMENT="production"

# Python settings (already set in Dockerfile but can be overridden)
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1
